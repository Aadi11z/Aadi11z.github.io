# Production origin, without the deployment subpath.
PUBLIC_SITE_URL=https://aadi11z.github.io

# Empty for local root, GitHub user sites, and custom domains.
PUBLIC_BASE_PATH=

# `normal` runs the visitor flow. `design` holds the intro for visual/debug review.
PUBLIC_INTRO_MODE=normal
